{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <!-- **************** MAIN CONTENT START **************** -->
    <main>
        <section class="pt-0">
            <div class="container-fluid px-0">
                <div class="bg-light h-70px h-md-70px rounded-0">
                </div>
            </div>
            <div class="container mt-4">
                <div class="row">
                    <!-- left -->
                    <div class="col-sm-12 col-lg-6 d-md-flex align-items-center justify-content-center bg-primary bg-opacity-10 vh-lg-50">
                        <div class="p-3 p-lg-3">
                            <!-- Title -->
                            <div class="text-center">
                                <h2 class="fw-bold">BIENVENUE SUR LE SITE DE VOTRE FEDERATION</h2>
                                <p class="mb-0 h6 fw-light">Construisons une solidarité solide!</p>
                            </div>
                        </div>
                    </div>

                    <!-- Right -->
                    <div class="col-sm-12 col-lg-6 m-auto">
                        <div class="row px-3 py-5">
                            <div class="col-sm-12 col-xl-12 m-auto">
                                <h1 class="fs-4">Connexion à votre espace membre!</h1>
                                <p class="lead mb-4">Heureux de vous revoir!</p>

                                <!-- Form START -->
                                <form method="post">
                                    {% if error %}
                                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                    {% endif %}

                                    <!-- Email -->
                                    <div class="mb-4">
                                        <label for="exampleInputEmail1" class="form-label">Votre adresse email *</label>
                                        <div class="input-group input-group-lg">
                                            <span class="input-group-text bg-light rounded-start border-0 text-secondary px-3"><i class="bi bi-envelope-fill"></i></span>
                                            <input type="email" name="email" value="{{ last_username }}" class="form-control border-0 bg-light rounded-end ps-1" placeholder="E-mail" id="exampleInputEmail1" required>
                                        </div>
                                    </div>
                                    <!-- Password -->
                                    <div class="mb-4">
                                        <label for="inputPassword5" class="form-label">Mot de passe *</label>
                                        <div class="input-group input-group-lg">
                                            <span class="input-group-text bg-light rounded-start border-0 text-secondary px-3"><i class="fa fa-lock"></i></span>
                                            <input type="password" name="password" class="form-control border-0 bg-light rounded-end ps-1" placeholder="password" id="inputPassword" required>
                                        </div>
                                        <div id="passwordHelpBlock" class="form-text">
                                            Votre mot de passe doit contenir au minimum 8 charactères
                                        </div>
                                    </div>
                                    <!-- Check box -->
                                    <div class="mb-4 d-flex justify-content-between mb-4">
                                        <div class="form-check">
                                            <input type="checkbox" class="form-check-input" id="exampleCheck1">
                                            <label class="form-check-label" for="exampleCheck1">Se souvenir de moi</label>
                                        </div>
                                        <div class="text-primary-hover">
                                            <a href="" class="text-secondary">
                                                <u>Mot de passe oublié ?</u>
                                            </a>
                                        </div>
                                    </div>
                                    <!-- Button -->
                                    <div class="align-items-center mt-0">
                                        <div class="d-grid">
                                            <button class="btn btn-dark bg-pink mb-0" type="submit">Connexion</button>
                                        </div>
                                    </div>
                                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                </form>
                                <!-- Form END -->
                                <!-- Sign up link -->
                                <div class="mt-4 text-center">
                                    <span>Vous n'avez pas encore de compte? <a href="">Créer un compte</a></span>
                                </div>
                            </div>
                        </div> <!-- Row END -->
                    </div>
                </div> <!-- Row END -->
            </div>
        </section>
    </main>
    <!-- **************** MAIN CONTENT END **************** -->


{% endblock %}


{% block footer %}
{% endblock %}