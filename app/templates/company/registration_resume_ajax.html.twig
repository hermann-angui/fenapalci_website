{#<div class="d-flex justify-content-between align-content-center align-items-center mt-5">
    <div class="col-md-3 shadow-sm h-100px text-center pt-3">
        <h6>Frais Etablissement</h6>
        <h3>{{ company.fee }}</h3>
    </div>
    <div class="col-md-1 h-100px  text-center pt-3">
        <h6></h6>
        <h1 class="mt-2">+</h1>
    </div>
    <div class="col-md-3 shadow-sm h-100px text-center pt-3">
        <h6>Frais employés</h6>
        <h3 class="mt-2">{{ employeeTotal }}</h3>
    </div>
    <div class="col-md-1 h-100px text-center pt-3">
        <h6></h6>
        <h1 class="mt-2">=</h1>
    </div>
    <div class="col-md-3 shadow-sm h-100px bg-purple text-center pt-3 text-white">
        <h6 class="text-white">TOTAL</h6>
        <h3 class="text-white mt-2">{{ total }}</h3>
    </div>
</div>#}
<div class="table-responsive border-0 mt-5">
    <table class="table table-dark-gray align-middle p-4 mb-0 table-sm">
        <tbody>
        <tr class="bg-light">
            <td><strong>{{ company.name|upper }}</strong></td>
            <td>{{ company.type=='COMPANY'? 'Etablissement' : '' }}</td>
{#            <td>{{ company.fee }}</td>#}
        </tr>
        {% for employee in employees %}
        <tr>
            <td><strong>{{ employee.name|upper }}</strong></td>
            <td>{{ employee.type=='STAFF' ? 'Employé' : '' }}</td>
{#            <td>{{ employee.fee }}</td>#}
        </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
<!-- Step 3 button -->
<div class="d-flex justify-content-end mt-5">
    <a href="{{ path('member__edit') }}" class="btn btn-primary bg-dark-purple next-btn mb-0">Revenir à mon profil</a>
</div>
{#
<div class="d-flex justify-content-end mt-5">
    <a href="{{ path('app_payment_summary', {'amount': total,'pay_for': 'FRAIS_ADHESION','beneficiary_id': company.id,'beneficiary_type': 'COMPANY'}) }}" class="btn btn-primary bg-dark-purple next-btn mb-0">Lancer le paiement</a>
</div>
#}
